# This sets the HTTP port for the embedded web server.
server.port=8000

# This gives the application a friendly name inside Spring.
spring.application.name=chapter01-spring-intro

# Disable template caching during learning so HTML changes appear immediately.
spring.thymeleaf.cache=false
